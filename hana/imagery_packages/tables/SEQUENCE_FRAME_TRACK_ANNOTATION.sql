CREATE COLUMN TABLE SEQUENCE_FRAME_TRACK_ANNOTATION 
(
	ANNOTATION_UUID VARCHAR(36) NOT NULL,
	ANNOTATION_PACKAGE_UUID VARCHAR(36) NOT NULL,
	IMAGERY_PACKAGE_UUID VARCHAR(36) NOT NULL,
	SEQUENCE_FRAME_UUID VARCHAR(36) NOT NULL,
	SEQUENCE_CLIP_FILENAME VARCHAR(200),
	SEQUENCE_CLIP_FILENAME_ID  VARCHAR(36),
	SEQ_FRAME INTEGER,
	PROJECT_ID VARCHAR(36),
	OBJECT_NAME NVARCHAR(100) NOT NULL,
	OBJECT_ID VARCHAR(36),
	TRACK_ID VARCHAR(36),
	ANNOTATION_ID VARCHAR(36),
	ANNOTATION_TYPE VARCHAR(20) NOT NULL,
	ANNOTATION_GEO_POLYGON ST_GEOMETRY(0),
	COORDS VARCHAR(400) NOT NULL,
	WIDTH_PIXELS INTEGER,
	HEIGHT_PIXELS INTEGER,
	AREA_PIXELS INTEGER,
	IS_KEY_FRAME BOOLEAN,
	LABEL_NAME NVARCHAR(100) NOT NULL,
	LABEL_ID VARCHAR(36),
	LABEL_KEY VARCHAR(100),
	CONFIDENCE INTEGER,
	PRIMARY KEY(ANNOTATION_UUID)
)